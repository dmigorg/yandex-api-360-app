/** OAuth access restriction status for the organisation */
export interface OauthRestrictionStatus {
  /** `true` — third-party OAuth app authorisation is restricted; `false` — no restrictions */
  restricted: boolean;
}
